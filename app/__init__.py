# app/__init__.py
# Package marker for the app package.
# Nothing fancy here — just keep this file so `python -m app.cli` works.
# You can add package-level docstrings or imports if needed in the future.
# For now, it's intentionally left empty.