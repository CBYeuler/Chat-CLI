# client/__init__.py
# Package marker for the client package.
# Nothing fancy here — just keep this file so `python -m client.cli` works.
# You can add package-level docstrings or imports if needed in the future.
# For now, it's intentionally left empty.